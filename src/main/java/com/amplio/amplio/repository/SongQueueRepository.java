package com.amplio.amplio.repository;

import com.amplio.amplio.models.SongQueue;
import org.springframework.data.repository.CrudRepository;

public interface SongQueueRepository extends CrudRepository<SongQueue, Integer> {
}
